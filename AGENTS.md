# AGENTS.md — 机器视觉知识库维护说明（家规）

> 本文件是知识库的 schema：告诉维护者（LLM，当前为 Codex）如何组织、维护和扩展这个 wiki。
> 它不是一成不变的——主人和 LLM 在使用过程中共同演化它。
> （2026-09-03 起，原 CLAUDE.md 内容已并入本文件；2026-09-08 起目录全面中文化。）

## 死规矩（主人要求，不可违反）

1. **先别动手构建**——和主人交互时一次只问一个问题，像真实对话伙伴，不一次性甩一堆。
2. **结构级改动先给方案等确认**——需求收集完、目录/结构方案给主人确认，主人明确回复"行"才动手搭建；任何结构级改动都先给方案等确认。

## 知识库定位

- 主人：**大程**，机器视觉工程师（图像处理 / 算法 / 软件开发 / 视觉系统搭建）。
- 目标：让工作更轻松——做项目时快速复用已有方案、选型、踩坑经验，避免重复劳动。
- 形态：Karpathy Wiki 模式（持久化、持续复利的个人知识库）。用 Obsidian 打开本文件夹（`F:\我的知识库`）即为 vault。
- 远程：GitHub 仓库 `cheng-wj/MyKnowledgeBase`，本文件夹即仓库根。

## 三层结构（知识库主体在 `知识库/`）

- **`知识库/原始资料/`**：原始资料（`论文/` `文章/` `规格书/` `图片/` `代码/`）。**只进不改**，是真相来源。
- **`知识库/知识页/`**：LLM 生成维护的 markdown 知识层。LLM 全权负责，主人只读不写。
  - `算法/` 算法与算子 ｜ `项目/` 项目经验与踩坑 ｜ `硬件/`（`相机/` `镜头/` `传感器/` `光源/` `采集卡/`）
  - `系统/`（`架构/` `通信/` `部署/`）｜ `概念/` 核心概念 ｜ `实体/` 实体 ｜ `综合分析/` 综合分析
  - `index.md` 内容目录 ｜ `log.md` 详细操作流水（只追加）｜ `overview.md` 知识总览
- **`知识库/附件/`**：图片附件。
- **`AGENTS.md`（本文件，仓库根）**：结构、约定与工作流说明。
- **`README.md`（仓库根）**：GitHub 首页，含"内容更新记录"。

## 核心工作流

### Ingest（摄入新资料）
1. 资料存到 `知识库/原始资料/` 对应子目录。
2. 读内容，与主人讨论要点。
3. 在 `知识库/知识页/` 对应分类下建/更新页面（算法→`算法/`，项目→`项目/`，硬件→`硬件/`，系统→`系统/`）。
4. 更新 `知识页/index.md`、追加 `知识页/log.md`（格式：`## [YYYY-MM-DD] ingest | 标题`，链接用相对 `知识页/` 的路径）。
5. **同步更新根目录 `README.md` 的"内容更新记录"**（最新的在最上面，带页面链接），每条记录末尾标注来源设备 `[@称呼]`。
6. 汇报触及了多少页面。

### Query（提问）
先读 `知识页/index.md` 找相关页 → 深入阅读 → 综合回答并引用来源 → 有持续价值的产物（对比分析、主题梳理）存回 `知识页/综合分析/`，并追加 `知识页/log.md`。

### Lint（健康检查）
扫描矛盾、过时声明、孤立页、文中提到但未建页的概念、缺失交叉引用；自动修能修的，报告需人工判断的；追加 `知识页/log.md`。

## Git 与同步约定

- 内容改动后可以本地 `git add -A` 和 `git commit`。
- **`git push` 推送到 GitHub 之前，必须先询问主人，经明确同意后才推送**，不得擅自推送。
- 提交信息用中文简述本次改动（如 `ingest: 摄入 xxx`、`docs: ...`）。

## 多设备识别（按电脑名区分上传/录入设备）

本仓库通过 Git 在多台设备间同步。每次开始工作前，先用 `hostname`（PowerShell `$env:COMPUTERNAME`）查当前设备，按下表确认身份；遇到未登记的新设备，先查 hostname 再把它补进本表。

| 主机名 | 称呼 | 用户 / 平台 | 仓库路径 | git 作者名 | 备注 |
|---|---|---|---|---|---|
| `DESKTOP-R4RRO1S` | 公司机 | ZY / Windows | `F:\我的知识库` | `大程·公司机` | Python 用 `py` 启动器（`python` 为商店占位符不可用）；git 走代理 `127.0.0.1:7897` |

设备标记约定：
- 每台设备设独立 git 作者名 `大程·<称呼>`（邮箱统一 `cheng-wj@users.noreply.github.com`，仍关联 GitHub 账号）；
- README「内容更新记录」每条、`知识库/知识页/log.md` 每条记录，末尾标注来源设备，格式 `[@称呼]`（如 `[@公司机]`）。

### 新设备首次接入流程（agent 读到本文件须自动执行）

在任何设备上开始实质性工作（摄入资料、改动文件）**之前**，先 `hostname`（PowerShell 用 `$env:COMPUTERNAME`）查当前主机名，对照上表：

1. **本机已在表中**：核对 git 作者名——`git config user.name` 应为 `大程·<本机称呼>`；若不对（如还是 `cheng-wj` 或别的设备名），执行 `git config user.name "大程·<本机称呼>"` 修正（邮箱保持 `cheng-wj@users.noreply.github.com`）。确认仓库路径与表中一致后即可正常工作。
2. **本机不在表中（新设备）**：
   - 先自己查好：主机名、用户名、操作系统平台（Windows / macOS / Linux）、本机仓库绝对路径；
   - 按"一次只问一个问题"的家规，**只问主人一个问题**：这台设备怎么称呼（如 个人机 / 笔记本 / 家里台式…）；
   - 得到称呼后自动完成：① 在上表追加一行（主机名 / 称呼 / 用户·平台 / 仓库路径 / git 作者名 `大程·<称呼>` / 备注）；② 执行 `git config user.name "大程·<称呼>"`；③ 在 `AGENTS.md_修改记录.md` 追加一条"登记新设备"记录；④ 本次及以后的 README、log 记录一律用 `[@称呼]` 标注；
   - **不要**在未登记、未设置本机作者名的情况下直接提交内容。
3. 每次换设备后的第一次提交前，用 `git log -1 --pretty=%an` 确认作者已是本机的 `大程·<称呼>`。

## 页面格式

- 论文/算法/概念/项目页用 YAML frontmatter（title/authors/year/tags/ingested 等）。
- 页面间用 Obsidian `[[wikilinks]]` 互链。
- `index.md` 每行：`[页面名](相对路径) — 一行摘要`。
- `log.md` 每条前缀：`## [YYYY-MM-DD] <动作> | <标题>`，文件链接用 Markdown 标准链接（VSCode/Obsidian 预览可点击）。

## 防遗漏（项目页强制栏目）

每个 `知识库/知识页/项目/*.md` 必须包含 `_template.md` 的五个栏目：
**关键参数与标定结果、硬件选型 BOM、踩坑与 root cause、客户/产线特殊约定、相关代码/算子**。

## 公开仓库注意事项

- 本仓库当前为公开。摄入真实项目资料时：**客户名/产线/验收口径一律用代号**（如"客户A-锂电产线"），公司机密参数与代码不放入 `知识库/原始资料/`。

## Obsidian 提示

- 打开仓库根文件夹即为 vault。
- 启用 Dataview 插件，可对页面 frontmatter 跑查询，生成动态表格。
- 图片放 `知识库/附件/`，页面用标准相对链接引用。

## 修改记录要求

- 本文件（AGENTS.md）的每次改动，都必须在同目录 **[AGENTS.md_修改记录.md](AGENTS.md_修改记录.md)** 追加一条，按"修改文件 / 修改内容 / 修改思路 / 验证情况"四段书写。
- 家规正文保持精简，改动历史统一放在修改记录文件里，不在本文件内联。
- 其他配置/脚本/结构文件若有持续维护需要，参照同样方式在同目录建 `源文件名_修改记录.md`。
